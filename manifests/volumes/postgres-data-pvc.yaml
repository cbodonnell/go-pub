apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: go-pub-postgres-data
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 500Mi
  selector:
    matchLabels:
      type: postgres